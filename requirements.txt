# Root requirements.txt — pip-installable dependencies for CI.
# ViennaRNA (RNAfold) is conda-only and NOT listed here; it is mocked in tests.
# For the full pipeline environment use: conda env create -f envs/zorc_pipeline.yml

# API runtime
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
shap>=0.45.0
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=7.0.0

# API tests
httpx>=0.27.0

# CI — linting
ruff>=0.4.0

# CI — test runner
pytest>=8.0.0
pytest-cov>=5.0.0
